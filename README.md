# LuminaBot
Illuminates knowledge from any input

## try.py
This file contains a function `is_prime(n)` that checks if a given number n is prime. It takes an integer as input and returns True if the number is prime, and False otherwise. The script also prompts the user to enter a number and then prints whether the number is prime or not.
